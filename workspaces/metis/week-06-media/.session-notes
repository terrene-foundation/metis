# Session Notes — 2026-04-30

## Where we are

Week 6 (MosaicHub Content Moderation) is now fully pre-staged for tonight's
workshop. Prior session shipped the scaffold (`075e11c`); this session
pre-ran `/analyze` and `/todos` so students open `claude`, paste the
START_HERE.md §7 boot prompt, and jump straight to Sprint 1 Vision boot
(`playbook/workflow-03-sprint-1-vision-boot.md`). Pushed as `f462afe`.

## Read first

1. `workspaces/metis/week-06-media/01-analysis/failure-points.md` —
   inheritance audit; 12 failure points cited to src/media/backend
2. `workspaces/metis/week-06-media/01-analysis/decisions-open.md` —
   23 open decisions tagged to Playbook phase, 5 ★ Trust-Plane moments
3. `workspaces/metis/week-06-media/todos/active/` — 21 phase todos
   (phase_1..3, vision 4..8, text 4..8, fusion 5, queue 10/11/12 +
   postimda × 2, drift 13, close 99)
4. `workspaces/metis/week-06-media/START_HERE.md` §7 — student opening
   prompt (boots backend + viewer + preflight)
5. `workspaces/metis/week-06-media/playbook/README.md` — file-by-file
   run order (22 files); §1 table is the navigation spine
6. `git log -1 f462afe` — body explains exactly what was pre-produced

## In-flight state

(none — fully committed and pushed)

## Traps

- Students will assume `/analyze` and `/todos` still need to run. Tell
  Claude Code "the analysis + todos files are pre-produced; copy them
  as the basis and proceed straight to Sprint 1 boot" in the opener.
- `journal/.pending/1777544054615-0-DECISION.md` is auto-generated by
  the SessionEnd hook from the prior commit. Promote-or-discard at
  start of next workshop, otherwise it'll confuse the journal scan.
- Backend startup still requires `.venv/bin/python src/media/scripts/generate_data.py`
  if the data dir is empty. The PRE-BUILT 24k procedural PNGs are 94 MB.
- Auto-detection in `routes/state.py::_detect_completed_phases` treats
  any `journal/phase_*.md` >500 bytes as authored. Smoke-tests that
  write fake journals will toggle banner sprints/decisions green.
- IMDA injection mutates `queue_constraints.json` in the workspace;
  `scripts/scenario_inject.py imda_csam_mandate --undo` reverts.
- The framework-first hook excludes `src/{retail,media,supply_chain}/`
  + matching `apps/web/` paths. Other paths still BLOCK raw FastAPI/sklearn.

## Open questions for the human

- `/sync` SDK pins moved last session (kailash-ml 0.17 → 1.6 etc.).
  Week 4/5/6 backends use no kailash imports so they're unaffected;
  flag if anything else in the repo starts importing kailash.
