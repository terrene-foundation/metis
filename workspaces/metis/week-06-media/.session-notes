# Session Notes — 2026-04-30

## Where we are

Week 6 (MosaicHub Content Moderation) shipped end-to-end. Backend boots in
~8 s, all 33 endpoints respond, IMDA injection + CSAM hard-floor + auto-
detected /state/current all verified. Pushed as `075e11c` to
`terrene-foundation/metis@main`. Working tree is clean. The commit also
applied the v2.8.31+ /sync output that was pending at session start, so
`.claude/` tooling is current (hooks live at `.claude/hooks/`, not
`scripts/hooks/`).

## Read first

1. `workspaces/metis/week-06-media/PRODUCT_BRIEF.md` — domain anchor: cost
   table, 4 sprints, 5 decision moments, IMDA mid-injection
2. `workspaces/metis/week-06-media/SCAFFOLD_MANIFEST.md` § "Implementation
   deviations" — explains the sklearn-on-Gaussian-modes surrogate for the
   ResNet/BERT framing, and why 32×32 procedural PNGs ship instead of real images
3. `src/media/backend/ml_context.py` — load-bearing invariants
   (CSAM_ADJACENT_HARD_FLOOR=0.40, drift_baselines_registered==3,
   IMAGE_CLASSES / TEXT_CLASSES tuples, _class_modes geometry rationale)
4. `src/media/backend/routes/state.py` — newest module: dynamic banner +
   journal auto-detection + 5-decision auto-resolve
5. `apps/web/media/index.html` — viewer; 6 cards + dynamic pipeline
6. `git log -1` for the commit body — covers what /sync moved + what Week 6 added

## In-flight state

(none — fully committed and pushed)

## Traps

- Backend startup fails if data is missing. Run
  `.venv/bin/python src/media/scripts/generate_data.py` first (~5 s).
- `data/images/` is 94 MB (24k 32×32 PNGs). They are PRE-BUILT scaffold
  artefacts; never edit. `--no-images` regenerates everything else fast.
- Dynamic banner auto-detects from `journal/phase_*.md` files >500 B.
  Smoke-tests that write fake journals will toggle sprints/decisions
  green — clean those up afterwards (cf. queue_constraints.json,
  .workshop_state.json, drift_retrain_rules.json in workspace).
- CSAM-adjacent hard floor (0.40) enforced TWICE — at threshold POST AND
  promote. Both 422 with audit-grade messages.
- IMDA injection mutates `queue_constraints.json` in the workspace;
  `scenario_inject.py imda_csam_mandate --undo` reverts.
- The framework-first hook excludes `src/{retail,media,supply_chain}/`
  + matching `apps/web/`. Other paths still BLOCK on raw FastAPI/sklearn.
- RF macro_f1 lands last on both modalities (image 0.61, text 0.54). This
  is intentional — high-dim noisy multi-mode geometry. Defensible Phase 5
  pedagogy. Do not "fix" without changing the embedding signal.

## Open questions for the human

- `/sync` SDK pins moved (kailash-ml 0.17 → 1.6 etc.). Week 4/5 backends
  use no kailash imports so they're unaffected, but if anything else in
  the repo does, those callers may need attention next.
